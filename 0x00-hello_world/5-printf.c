#include <stdio.h>

/**
 * main - Entry point
 * Description: using the printf function to display a string
 * Return: 0 (success)
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
