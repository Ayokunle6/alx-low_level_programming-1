#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * add - Entry point(calling function from main.h file)
 * Description: Summing two integers together
 * @a, @b: variables for the integers
 * Return: 0 (success)
 */

int add(int a, int b)
{
	int sum = (a + b);
	return (sum);
}
